---
import {site} from '../../site';
import prismcss from './prism-vs.css?raw';
import styles from './styles.css?raw';

type Amp_Include = keyof typeof site.amp_src;

interface Props {
  amp_include: Amp_Include[];
  title: string;
  description: string;
  slug: string;
}

const { amp_include, title, description, slug } = Astro.props;
---
<!DOCTYPE html>
<html amp lang="en">
<head>
<link href='https://fonts.googleapis.com/css?family=Open+Sans:400,300,700' rel='stylesheet' type='text/css'>
<link href='https://fonts.googleapis.com/css?family=Lora:400,700' rel='stylesheet' type='text/css'>

<script async custom-element="amp-analytics" src={site.amp_src['amp-analytics']}></script>
{amp_include.map(amp_element=> (
<script async custom-element={amp_element} src={site.amp_src[amp_element]}></script>
))}

<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width,minimum-scale=1">
<meta name="description" content={ description}>
<link rel="canonical" href={`${site.url}${slug}`}>
<title>{title} | {site.name}</title>
<style amp-custom is:inline set:html={`
${prismcss}
${styles}
`}></style>

<style is:inline amp-boilerplate>body{-webkit-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-moz-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-ms-animation:-amp-start 8s steps(1,end) 0s 1 normal both;animation:-amp-start 8s steps(1,end) 0s 1 normal both}@-webkit-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-moz-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-ms-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-o-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}</style><noscript><style amp-boilerplate>body{-webkit-animation:none;-moz-animation:none;-ms-animation:none;animation:none}</style></noscript>
<script async src="https://cdn.ampproject.org/v0.js"></script>
</head>

<body>
<amp-analytics type="googleanalytics" id="analytics">
<script type="application/json">
{
  "vars": {
    "account": "UA-78463139-1"
  },
  "triggers": {
    "trackPageview": {
      "on": "visible",
      "request": "pageview"
    }
  }
}
</script>
</amp-analytics>

<header class="site-header">
<a href="/" class="logo"><span class="logo"><amp-img src={ site.logo } width="48" height="48" /></span></a>
<div class="page-links">
<a class="page-link" href="/">Home</a><span> • </span><a class="page-link" href="/about">About</a>
</div>
</header>

<slot></slot>

</body>
</html>
